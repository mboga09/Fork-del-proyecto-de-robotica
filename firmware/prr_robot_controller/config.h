#pragma once

#include <Arduino.h>

constexpr unsigned long BAUD_RATE = 115200;

// Dummy duration only used until real sensors/motors are connected.
constexpr unsigned long DUMMY_HOMING_DURATION_MS = 3000;